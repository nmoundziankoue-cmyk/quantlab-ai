apiVersion: v1
kind: Secret
metadata:
  name: apexquant-secrets
  namespace: apexquant
type: Opaque
# Base64-encoded values — replace before deploying
# echo -n 'your-secret' | base64
stringData:
  DATABASE_URL: "postgresql://apex:apex@postgres-svc:5432/apexquant"
  REDIS_URL: "redis://redis-svc:6379/0"
  JWT_SECRET_KEY: "change-me-in-production"
